set(INCLUDE checker_lib/checker.h)
set(SOURCE  checker_lib/checker.cpp)
add_library(checker_lib STATIC ${SOURCE} ${INCLUDE})
include_directories(checker_lib/)
add_executable(checker checker_demo.cpp)
target_link_libraries(checker checker_lib)
